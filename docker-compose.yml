networks:
  niddle-network:
    name: niddle-network
    driver: bridge

services:
  postgres:
    image: docker.io/library/postgres:16
    container_name: niddle-postgres
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: niddle
      POSTGRES_PASSWORD: niddle
      POSTGRES_DB: niddle_db
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U niddle -d niddle_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - niddle-network

  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: api-dev
    image: niddle-api:dev
    container_name: niddle-api
    restart: unless-stopped
    ports:
      - '3001:3001'
    environment:
      DATABASE_USER: niddle
      DATABASE_PASSWORD: niddle
      DATABASE_MASTER: postgres
      DATABASE_SLAVE: postgres
      DATABASE_PORT: 5432
      DATABASE_NAME: niddle_db
      DATABASE_SYNCHRONIZE: "true"
      PORT: 3001
      JWT_SECRET: very_secure_secret
      ADMIN_EMAIL: admin@example.com
      ADMIN_PASSWORD: "12345678"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/api/node_modules
      - /app/apps/web/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - niddle-network

  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: web-dev
    image: niddle-web:dev
    container_name: niddle-web
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      VITE_API_URL: http://api:3001
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/api/node_modules
      - /app/apps/web/node_modules
    depends_on:
      - api
    networks:
      - niddle-network

volumes:
  pgdata:

